{
  "name": "realtime-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Generalized realtime WebSocket hub powered by Cloudflare Durable Objects and Queues",
  "scripts": {
    "dev": "wrangler dev --port 8790",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260601.1",
    "@types/node": "^25.9.1",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8",
    "wrangler": "^4.94.0"
  }
}
